[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devlog-cli"
version = "1.0.0"
description = "Catat progress harianmu langsung dari terminal"
authors = [{name = "irham-s-a"}]
requires-python = ">=3.10"
dependencies = ["click", "rich"]

[project.scripts]
devlog = "devlog.cli:cli"
